DATABASE_URL="postgresql://flowboard:flowboard@localhost:5432/flowboard?schema=public"
# Optional direct connection URL for Prisma migrations (recommended for Neon).
DIRECT_URL=""
JWT_SECRET="replace-with-a-long-random-secret"
PORT=4000
FRONTEND_URL="http://localhost:3000"
# Separate multiple deployed frontend origins with commas.
